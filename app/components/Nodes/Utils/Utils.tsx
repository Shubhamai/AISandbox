import {  FormInput } from "lucide-react";
import CreatePromptNode from "./CreatePrompt";

export const utilsNodesData = {
  CreatePromptNode: {
    name: "Create Prompt",
    description: "Create a prompt",
    icon: <FormInput />,
  },
};

export const utilsNodes = { CreatePromptNode };
