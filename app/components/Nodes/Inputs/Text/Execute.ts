import { Node } from "reactflow";

const executeInputTextNode = (node: Node, previousNode: Node) => {
  //   if (previousNode.type === "TextOutputNode") {
  return node;
  //   }
};

export default executeInputTextNode;
