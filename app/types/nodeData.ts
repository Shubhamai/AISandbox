type nodeData = {
  label: any;

  // TODO : Default data now lives, might need to be moved
  input: {
    text: any;
    image: any;
  };
  output: {
    text: any;
    image: any;
  };
  hasComputed: boolean;
};
