### Creating a new Node

Creating a new node to add to the editor requires a few steps. In this example, we are going to create a node which takes a string as input and outputs a string with the same value.

#### 1. Create a new class

**..In Progress**